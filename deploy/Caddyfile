{
    email {$ACME_EMAIL}
}

www.buenzli.space {
    redir https://buenzli.space{uri} permanent
}

buenzli.space {
    encode gzip

    # Landing-page contact form → FastAPI backend.
    handle_path /zuribot/* {
        reverse_proxy zuribot:8000
    }

    handle {
        reverse_proxy web:80
    }
}
