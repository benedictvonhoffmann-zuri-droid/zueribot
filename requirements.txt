# App dependencies — install after the heavy ML layer.
# Adding packages here does NOT invalidate the torch/sentence-transformers cache.
langchain-ollama>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0
python-dotenv>=1.0.0
mcp>=1.0.0
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
sse-starlette>=2.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
defusedxml>=0.7.0
pandas>=2.0.0
PyJWT[crypto]>=2.8.0
httpx>=0.27.0
# KB Phase 1 — tokenizer only, used for counting (not embedding).
tokenizers>=0.15.0
